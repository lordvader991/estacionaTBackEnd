eliminar el venv y hacer uno nuevo
python -m venv venv
activar el entorno virtual
venv\Scripts\activate.bat (hay otras formas)
.\venv\Scripts\activate

instalar librerias:
pip install django djangorestframework
pip install pymysql
pip install django

cd demo
django-admin startapp el nombre de la rama
con ello crear los archivos serializers y urls
modificar models y views


como hacer insercion con postman en el metodo POST (depende de tabla para insertar)
{
    "brand": "Toyota",
    "model": "Corolla",
    "registration_plate": "ABC123",
    "type_vehicle": 1,  // ID del TypeVehicle relacionado
    "user": 1  // ID del User relacionado
}

prender el xampp para que se pueda ver por local la api
y tambien darle makemigrations y migrate cuando ya se haya hecho modificaciones









